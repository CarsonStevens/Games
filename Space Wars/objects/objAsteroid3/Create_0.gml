/// @description Variable Setup

//kinemetics
x_vel = random_range(1,30);
y_vel = random_range(1,30);
x_acc = 0;
y_acc = 0;

//For size and rotation
rotation = random_range(0,10);
size = random_range(0,2);
image_xscale = size;
image_xscale = size;


